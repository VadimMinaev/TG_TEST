# Telegram Webhook Integration — Полное описание функционала

Небольшое приложение для приёма вебхуков, фильтрации их по правилам и отправки сообщений в Telegram. Поддерживает как файловое хранение (локально), так и работу с PostgreSQL. Содержит веб-интерфейс для управления правилами, логами, очередью сообщений и пользователями.

**Содержание**
- **Кратко**: что делает проект
- **Бэкенд**: все эндпоинты и поведение
- **Фронтенд**: какие возможности UI предоставляет
- **Режимы хранения**: файлы vs PostgreSQL
- **Очередь сообщений и воркер**
- **Аутентификация и управление пользователями**
- **Переменные окружения и запуск**
- **Примеры**

**Кратко**
- Приходит POST на `/webhook` — приложение последовательно проверяет все включённые правила. Каждое правило содержит JavaScript-условие (переменная `payload` — данные вебхука), токен бота и один или несколько `chatId`.
- При совпадении формируется текст сообщения (автоматический шаблон или пользовательский шаблон) и ставится в очередь на отправку в Telegram (или отправляется прямо в файловом режиме).
- Веб-интерфейс позволяет создавать/редактировать/удалять правила, тестировать условия, отправлять тестовые сообщения, просматривать логи и состояние очереди.

**Бэкенд — функционал и эндпоинты**
- Сервис: Express приложение — `server/app.js`.
- Статический фронтенд: `public/index.html`.

- Авторизация
  - `POST /api/login` — логин (поддерживается встроенный `vadmin` с паролем по умолчанию в коде и пользователи из БД). Возвращает токен-сессию с истечением через 24 часа.
  - `POST /api/logout` — выходит из сессии.
  - `GET /api/me` — информация о текущем пользователе.
  - `GET /api/auth-status` — проверка авторизации с автоматической обработкой истечения сессии.

- Управление пользователями (требует PostgreSQL)
  - `GET /api/users` — список пользователей.
  - `POST /api/users` — создать пользователя (только `vadmin`).
  - `PUT /api/users/:id/password` — смена пароля (пользователь сам или `vadmin`).
  - `DELETE /api/users/:id` — удалить пользователя (только `vadmin`).

- Управление токеном бота
  - `POST /api/bot-token` — валидация и сохранение глобального токена бота (используется как резерв для правил без собственного токена).
  - `GET /api/bot-token` — вернуть замаскированный глобальный токен и признак установленности.

- Тестовая отправка
  - `POST /api/test-send` — отправка тестового сообщения в указанный `chatId` (можно указать временный `botToken`).

- Управление правилами (CRUD)
  - `GET /api/rules` — получить все правила (включая created_at и updated_at timestamps).
  - `GET /api/rules/:id` — получить правило по ID.
  - `POST /api/rules` — создать правило (в запросе: `name`, `condition`, `chatId`/`chatIds`, `botToken`, `messageTemplate`, `enabled`). При создании правило получает `authorId` (автором считается `vadmin` или текущий пользователь из БД) и timestamps.
  - `PUT /api/rules/:id` — обновить правило (только автор или `vadmin`). Обновляет updated_at timestamp.
  - `DELETE /api/rules/:id` — удалить правило (только автор или `vadmin`).

- Обработка вебхуков
  - `POST /webhook` — основной обработчик. При поступлении:
    - Поддерживается special event `webhook.verify` для проверки URL.
    - Формируется `incomingPayload` (обычно `req.body.payload` или `req.body`).
    - Загружаются активные правила (из файла или БД).
    - Для каждого правила создаётся функция `new Function('payload', 'return ' + condition)` и условие выполняется.
    - При совпадении формируется текст сообщения (функция форматирования учитывает `id`, `subject`, `requested_by`, SLA-поля, `note` и т.д.) и ставится в очередь отправки.
    - Возвращается JSON с количеством совпадений и результатами для Telegram.

- Логи и история
  - `GET /api/webhook-logs` — список логов вебхуков (файловый режим или БД).
  - `GET /api/webhook-logs/:id` — детальный лог по ID.
  - `DELETE /api/webhook-logs` — очистить логи.

- Очередь сообщений (требует PostgreSQL для полноценной очереди)
  - `GET /api/message-queue/status` — статус очереди (доступность, total, pending, stats).
  - `GET /api/message-queue/history?page=&limit=&status=` — история сообщений из очереди (пагинация, фильтр по статусу).

- Дополнительно (бэкенд)
  - Поддержка PostgreSQL: при наличии `DATABASE_URL` используется БД для правил, логов, пользователей, очереди сообщений и настроек.
  - Файловый режим: без `DATABASE_URL` правила и логи хранятся в `data/rules.json` и `data/logs.json`.
  - Очередь сообщений: при наличии БД сообщения добавляются в `message_queue`, воркер обрабатывает их и отправляет в Telegram, с retry, приоритетами и очисткой старых записей.
  - Rate limiting: по чату (приватные vs группы) и глобальный лимит (пример: 30 msg/s).
  - Маскирование токенов в выводах и UI для безопасности.

**Фронтенд — возможности UI (в `public/index.html`)**
- Аутентификация: экран логина; `vadmin` видит дополнительные возможности. Автоматическая обработка истечения сессий (24 часа) с уведомлением пользователя.
- Вкладки: Правила, Тестирование, История, Очередь сообщений, Пользователи (для vadmin).
- Управление правилами:
  - Список правил (поиск, фильтрация). Отображение времени создания и последнего обновления для каждого правила.
  - Просмотр деталей правила (мета, условие, настройки отправки, шаблон).
  - Создание/редактирование правила: визуальный конструктор условий и ручной режим ввода JavaScript. Исправлен баг с закрытием модала при добавлении условий (кнопки имеют type="button").
  - Поддержка нескольких `chatId` (внутри формы можно передавать через запятую или массив в бэке).
  - Поле `botToken` для каждого правила (можно иметь отдельного бота для правила).
  - Указание `messageTemplate` (если пусто — используется автоформатирование).
  - Включение/отключение правила (`enabled`).
  - Экспорт/импорт правил в JSON (экспорт файла с выбором через модальное окно, импорт заменяет текущие правила). Чекбоксы удалены из просмотра правил для упрощения интерфейса.
  - Дублирование существующих правил с кнопками в различных режимах просмотра.

- Тестирование
  - Встроенный тестер условий: локально выполняет `new Function('payload', condition)` и показывает TRUE/FALSE или ошибки.
  - Тестовая отправка в Telegram (`/api/test-send`) с выводом подробного результата.

- История и Логи
  - Список вебхуков, пагинация, просмотр детальной информации: payload, результаты отправки в Telegram.
  - Удаление всех логов из UI.

- Очередь сообщений
  - Просмотр статистики очереди и таблицы истории сообщений (ID, статус, chatId, текст, попытки, ошибки).
  - Фильтр по статусу и пагинация.

- Пользователи
  - Для `vadmin`: создание/удаление пользователей.
  - Пользователь может сменить свой пароль (через `/api/users/:id/password`).

- Утилиты UI
  - Тёмная/светлая тема, сохранение настройки в localStorage.
  - Превью Telegram-сообщения (форматирование по тому же алгоритму, что и на бэке).
  - Модальное окно с инструкцией и автоматически сгенерированным webhook URL.

**Режимы хранения**
- Файловый режим (по умолчанию):
  - `data/rules.json` — сохранённые правила.
  - `data/logs.json` — последние 100 логов вебхуков.
  - `data/settings.json` — глобальные настройки (например, глобальный токен бота).
  - Подходит для простого использования без БД.
- Режим с PostgreSQL (при наличии `DATABASE_URL`):
  - Таблицы: `rules`, `logs`, `users`, `settings`, `message_queue`.
  - В этом режиме включается очередь сообщений, управление пользователями, и сохранение глобального токена в DB.

**Очередь сообщений и воркер**
- При наличии БД сообщения добавляются в `message_queue` и обрабатываются воркером, который:
  - Выполняет отправку с учётом приоритета;
  - Реализует retry (max_attempts) и обработку ошибок (429 Too Many Requests — рестарт с задержкой);
  - Выполняет rate limiting по chatId и глобально;
  - Очищает старые записи (старше 7 дней) по расписанию.

**Безопасность и ограничения**
- Условия правил выполняются через `new Function('payload', 'return ' + condition)` — это удобно, но потенциально опасно. Используйте только доверенные выражения.
- В файловом режиме есть встроенный `vadmin` пользователь (логин/пароль в коде). Для продакшена рекомендуем использовать PostgreSQL и реальные пользователи.
- Токены ботов маскируются в UI и логах.

**Переменные окружения**
- `TELEGRAM_BOT_TOKEN` — глобальный бот (используется как fallback).
- `PORT` — порт сервера (по умолчанию 3000).
- `DATABASE_URL` — если указан, приложение использует PostgreSQL вместо файлов.
- `.env` можно создать из `.env.example`.

**Запуск и скрипты**
1. Установите зависимости:
```bash
npm install
```
2. В режиме разработки с автоперезагрузкой:
```bash
npm run dev
```
3. В продакшн режиме:
```bash
npm start
```

Сервер по умолчанию доступен на `http://localhost:3000`.

**Основные точки входа / Примеры запросов**
- Отправка вебхука (пример curl):
```bash
curl -X POST http://localhost:3000/webhook \
  -H "Content-Type: application/json" \
  -d '{"team_id":40,"category":"incident","subject":"Test"}'
```

- Получение правил:
```bash
GET /api/rules
Authorization: Bearer <token>
```

- Тест условия (локально в UI): поле `Тестирование` → введите условие и payload → `Протестировать условие`.

**Файлы и структура проекта**
- `server/app.js` — основной сервер и бизнес-логика.
- `public/index.html` — SPA интерфейс (HTML + CSS + JS).
- `data/` — `rules.json`, `logs.json`, `settings.json` для файлового режима.
- `package.json` — скрипты и зависимости.

**Советы по эксплуатации**
- Для высокой нагрузки используйте PostgreSQL + отдельный процесс/инстанс для воркера (можно вынести обработку очереди в отдельный сервис).
- Контролируйте токены и не храните их в открытых репозиториях.
- Используйте тестер условий перед сохранением сложных выражений.
- Используйте дублирование правил для быстрого создания похожих конфигураций.
- Регулярно проверяйте timestamps правил для отслеживания изменений.
- Мониторьте истечение сессий; приложение автоматически обрабатывает их, но пользователь получит уведомление.

Если нужно, я могу:
- добавить примеры API с curl для всех эндпоинтов;
- вынести форматирование сообщений в отдельную документацию;
- сгенерировать Postman коллекцию для тестирования API.

---

MIT
# 📱 Telegram Webhook Integration

Приложение для маршрутизации вебхуков в Telegram каналы с использованием пользовательских условий.

## ✨ Возможности

- 🎯 Создание правил маршрутизации с произвольными условиями (JavaScript)
- 📤 Отправка сообщений в разные Telegram каналы в зависимости от условий
- 🧪 Встроенный тестер для проверки условий
- 🖥️ Интуитивный веб-интерфейс для управления правилами
- 📊 Поддержка сложных условий (вложенные свойства, массивы, функции)
- 🔄 REST API для управления правилами
- 💾 Сохранение правил в JSON файл или PostgreSQL
- 📋 Очередь сообщений с воркером для надёжной доставки
- 👥 Управление пользователями и аутентификация
- 🌙 Тёмная/светлая тема интерфейса
- 👁️ Превью сообщений Telegram
- 📤 Экспорт правил с выбором через модальное окно
- 📋 Дублирование существующих правил
- ⏰ Отображение времени создания и обновления правил
- 🔐 Автоматическая обработка истечения сессий (24 часа)

## 🚀 Установка

### 1. Установите зависимости

```bash
npm install
```

### 2. Создайте Telegram бота

1. Откройте Telegram и напишите боту [@BotFather](https://t.me/botfather)
2. Используйте команду `/newbot`
3. Следуйте инструкциям и получите токен

### 3. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваш токен:

```
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
PORT=3000
WEBHOOK_SECRET=your_secret_key_here
```

### 4. Запустите сервер

**Development mode (с автоперезагрузкой):**
```bash
npm run dev
```

**Production mode:**
```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## 📝 Использование

### Веб-интерфейс

1. Откройте http://localhost:3000 в браузере
2. Заполните форму "Создать правило":
   - **Название** - описание правила
   - **Условие** - JavaScript выражение (переменная `payload` содержит данные вебхука)
   - **ID Telegram чата** - ID канала или группы (отрицательное число)
   - **Шаблон сообщения** (опционально) - кастомный формат сообщения

3. Нажмите "Сохранить правило"

#### Дополнительные возможности интерфейса
- **Просмотр правил**: Список с поиском и фильтрацией, отображение времени создания и последнего обновления.
- **Экспорт правил**: Модальное окно для выбора правил к экспорту в JSON.
- **Дублирование правил**: Кнопки для копирования существующих правил.
- **Конструктор условий**: Визуальный и ручной режимы, исправлен баг с закрытием модала.
- **Аутентификация**: Автоматическая обработка истечения сессий с уведомлениями.

### Примеры условий

```javascript
// Проверка простого значения
payload.team_id === 14

// Проверка строки
payload.status === 'error'

// Математическое сравнение
payload.priority > 5

// Вложенные свойства
payload.user.role === 'admin'

// Проверка массива
payload.tags.includes('urgent')

// Комбинированные условия
payload.team_id === 14 && payload.status === 'error'

// Проверка наличия свойства
payload.hasOwnProperty('custom_field')

// Регулярные выражения
/error/i.test(payload.message)

// Проверка времени
payload.timestamp > Date.now() - 3600000
```

### API Endpoints

#### Получить все правила
```bash
GET /api/rules
```
Возвращает список правил с timestamps (created_at, updated_at).

#### Создать новое правило
```bash
POST /api/rules
Content-Type: application/json

{
  "name": "Отправить в основной чат",
  "condition": "payload.team_id === 14",
  "chatId": "-1001234567890",
  "messageTemplate": "Новое событие: ${payload}",
  "enabled": true
}
```

#### Обновить правило
```bash
PUT /api/rules/:id
Content-Type: application/json

{
  "name": "Обновленное имя",
  "condition": "payload.team_id === 15",
  ...
}
```

#### Удалить правило
```bash
DELETE /api/rules/:id
```

#### Протестировать условие
```bash
POST /api/test-rule
Content-Type: application/json

{
  "condition": "payload.team_id === 14",
  "payload": {
    "team_id": 14,
    "status": "success"
  }
}
```

### Отправка вебхука

```bash
curl -X POST http://localhost:3000/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "team_id": 14,
    "status": "success",
    "message": "Deployment completed"
  }'
```

## 📂 Структура проекта

```
.
├── server/
│   └── app.js              # Express приложение
├── public/
│   └── index.html          # Веб-интерфейс
├── data/
│   └── rules.json          # Сохраненные правила
├── package.json            # Зависимости
├── .env.example            # Шаблон переменных окружения
└── README.md               # Этот файл
```

## 🔒 Безопасность

- Условия выполняются в изолированной функции с доступом только к `payload`
- Не рекомендуется использовать это в untrusted окружении с опасными условиями
- Всегда проверяйте условия в тестере перед сохранением
- Сессии истекают через 24 часа для безопасности
- Токены ботов маскируются в логах и UI

## 📋 Пример полного рабочего процесса

1. **Создать правило в UI:**
   - Название: "Отправить ошибки в #errors"
   - Условие: `payload.level === 'error'`
   - Чат ID: `-1001234567890`

2. **Отправить вебхук:**
   ```bash
   curl -X POST http://localhost:3000/webhook \
     -H "Content-Type: application/json" \
     -d '{"level": "error", "message": "Something went wrong"}'
   ```

3. **Результат:** Сообщение отправится в Telegram канал

## 🐛 Логирование

Все события логируются в консоль:
- Поступление вебхуков
- Совпадение правил
- Отправка сообщений в Telegram
- Ошибки выполнения

## 💡 Дополнительные возможности

### Шаблонизация сообщений

Используйте переменные в шаблоне:
```
Статус: {payload.status}
Сообщение: ${payload.message}
Полные данные:
${payload}
```

### Множественные условия

Одно правило может содержать сложное условие:
```javascript
(payload.team_id === 14 || payload.team_id === 15) && payload.priority > 5
```

### Timestamps для правил

Каждое правило имеет поля `created_at` и `updated_at`, отображаемые в UI для отслеживания изменений.

### Дублирование правил

Используйте кнопки дублирования в интерфейсе для быстрого создания копий существующих правил.

### Экспорт с выбором

Модальное окно экспорта позволяет выбрать конкретные правила для экспорта в JSON.

### Управление сессиями

Автоматическая обработка истечения сессий через 24 часа с уведомлениями пользователя.

### Использование встроенных функций JavaScript

```javascript
// Проверка количества элементов
payload.items.length > 0

// Поиск элемента
payload.users.some(u => u.id === 123)

// Фильтрация
payload.data.filter(d => d.active).length > 0
```

## 📞 Помощь

Если возникли проблемы:
1. Проверьте, что сервер запущен (`npm run dev`)
2. Убедитесь, что токен бота верный в `.env`
3. Протестируйте условие в встроенном тестере
4. Проверьте логи консоли для деталей ошибок

## 📄 Лицензия

MIT
