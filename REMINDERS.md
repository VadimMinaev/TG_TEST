# Модуль напоминаний (Reminder Engine)

Модуль напоминаний для Telegram-бота — аналог Skeddy. Позволяет создавать одноразовые и повторяющиеся напоминания через Telegram-бота.

## 📋 Возможности

- ✅ Автоматическое создание пользователя Telegram при первом обращении
- ✅ CRUD напоминаний (создание, просмотр, удаление)
- ✅ Одноразовые напоминания (на конкретное время/дату)
- ✅ Повторяющиеся напоминания (interval / cron)
- ✅ **Отдельный бот для напоминаний** (не глобальный токен)
- ✅ Настройка токена и webhook через UI
- ✅ Отправка через существующую очередь сообщений
- ✅ Логирование отправки напоминаний
- ✅ Веб-интерфейс для управления

## 🚀 Быстрый старт

### 1. Откройте настройки бота напоминаний

1. Откройте веб-интерфейс: `http://localhost:3000` (или ваш домен)
2. Логин: `vadmin`, пароль: `vadmin`
3. В левом меню нажмите **Напоминания** → кнопка **Настройки бота**
4. Или перейдите напрямую: `/reminders/settings`

### 2. Создайте бота для напоминаний

1. В Telegram: найдите @BotFather
2. Отправьте `/newbot`
3. Введите имя бота (например: `Напоминания`)
4. Введите username бота (должен заканчиваться на `bot`, например: `my_reminder_bot`)
5. Сохраните полученный токен: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 3. Настройте бота в приложении

1. В разделе **Настройки бота напоминаний**:
2. Вставьте токен в поле "Токен из @BotFather"
3. Нажмите **Сохранить**
4. После сохранения нажмите **Установить webhook**

### 4. Протестируйте

1. Откройте вашего бота в Telegram (например: `@my_reminder_bot`)
2. Отправьте `/start`
3. Бот должен ответить приветствием

## 📱 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/help` | Показать справку |
| `/remind` | Создать напоминание |
| `/myreminders` | Показать мои напоминания |
| `/delete` | Удалить напоминание |

## 📝 Создание напоминаний

### Простое напоминание (через N минут/часов/дней)

```
/remind 10m Купить молоко
/remind 1h Встреча с клиентом
/remind 1d Позвонить партнёру
/remind 2w Подготовить отчёт
```

**Обозначения:**
- `m` — минуты
- `h` — часы
- `d` — дни
- `w` — недели

### Напоминание на конкретную дату

```
/remind 2025-02-20 14:00 Совещание в конференц-зале
/remind 2025-03-15 09:00 День рождения коллеги
```

**Формат:** `YYYY-MM-DD HH:MM`

### Повторяющееся напоминание (интервал)

```
/remind every 1h Принять лекарство
/remind every 4h Проверить температуру
/remind every 1d Ежедневный отчёт
```

### Повторяющееся напоминание (cron)

```
/remind cron 0 9 * * * Утренняя зарядка
/remind cron 0 18 * * 1-5 Окончание рабочего дня
```

**Формат cron:** `минута час день месяц день_недели`

## 🔧 Архитектура

### База данных

#### Таблица `telegram_users`
```sql
CREATE TABLE telegram_users (
    id BIGSERIAL PRIMARY KEY,
    telegram_id BIGINT UNIQUE NOT NULL,    -- ID пользователя Telegram
    username VARCHAR(255),                  -- @username
    first_name VARCHAR(255),                -- Имя
    last_name VARCHAR(255),                 -- Фамилия
    language_code VARCHAR(10),              -- Язык (ru/en)
    is_bot BOOLEAN DEFAULT false,           -- Это бот?
    created_at TIMESTAMP,
    updated_at TIMESTAMP
)
```

#### Таблица `telegram_reminders`
```sql
CREATE TABLE telegram_reminders (
    id BIGSERIAL PRIMARY KEY,
    telegram_user_id BIGINT NOT NULL,       -- Ссылка на пользователя
    message TEXT NOT NULL,                   -- Текст напоминания
    run_at TIMESTAMP NOT NULL,               -- Время запуска
    repeat_type VARCHAR(20),                 -- none/interval/cron
    repeat_config JSONB,                     -- Конфигурация повтора
    is_active BOOLEAN DEFAULT true,          -- Активно ли
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    next_run_at TIMESTAMP                    -- Следующий запуск
)
```

#### Таблица `reminder_logs`
```sql
CREATE TABLE reminder_logs (
    id BIGSERIAL PRIMARY KEY,
    reminder_id BIGINT NOT NULL,
    telegram_user_id BIGINT NOT NULL,
    status VARCHAR(20),                      -- sent/failed/skipped
    message_text TEXT,
    error_message TEXT,
    sent_at TIMESTAMP
)
```

### Индексы

```sql
-- telegram_users
idx_telegram_users_telegram_id
idx_telegram_users_username

-- telegram_reminders
idx_telegram_reminders_user_id
idx_telegram_reminders_next_run_at (для активных)
idx_telegram_reminders_run_at

-- reminder_logs
idx_reminder_logs_reminder_id
idx_reminder_logs_user_id
idx_reminder_logs_sent_at
```

### Компоненты

```
┌─────────────────────────────────────────────────────────┐
│                    Telegram Bot API                     │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  /api/telegram/webhook (Express endpoint)               │
│  └── handleTelegramUpdate()                             │
│       ├── getOrCreateTelegramUser()                     │
│       └── handleTelegramCommand()                       │
│           ├── /start → handleStartCommand()             │
│           ├── /remind → handleRemindCommand()           │
│           ├── /myreminders → handleMyRemindersCommand() │
│           └── /delete → handleDeleteCommand()           │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              ReminderService (сервисный слой)           │
│  ├── createReminder()                                   │
│  ├── getUserReminders()                                 │
│  ├── deactivateReminder()                               │
│  └── parseReminderCommand()                             │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  Database (PostgreSQL)                                  │
│  ├── telegram_users                                     │
│  ├── telegram_reminders                                 │
│  └── reminder_logs                                      │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  reminder_dispatcher (worker, 30с)                      │
│  └── processDueReminders()                              │
│       └── sendDueReminder() → addMessageToQueue()       │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│  message_queue (существующая очередь)                   │
│  └── startMessageQueueWorker() (100мс)                  │
└─────────────────────────────────────────────────────────┘
```

## 🔌 API Endpoints

### Telegram Webhook

**POST** `/api/telegram/webhook`

Получает update от Telegram Bot API.

**Тело запроса:** Telegram Update object

**Ответ:**
```json
{ "ok": true }
```

### Web-интерфейс напоминаний

**GET** `/api/reminders`

Получение напоминаний для авторизованного пользователя.

**Ответ:**
```json
{
  "reminders": []
}
```

> ⚠️ **Примечание:** В текущей версии API возвращает пустой массив, так как напоминания привязаны к Telegram user ID, а не к пользователю веб-интерфейса. Для связи необходимо добавить механизм linking.

## 🎨 Веб-интерфейс

Страница напоминаний доступна по адресу `/reminders`.

**Функции:**
- Просмотр справки по командам
- Просмотр списка напоминаний (требует связи с Telegram)
- Быстрый старт для новых пользователей

## 📊 Мониторинг

### Проверка работы воркера

В логах приложения ищите сообщения:
```
[Reminder] Dispatcher started (checking every 30s)
[Reminder] Sent reminder <id> to user <telegram_id>
[Reminder] Created reminder <id> for user <user_id> at <date>
```

### Проверка базы данных

```sql
-- Количество пользователей
SELECT COUNT(*) FROM telegram_users;

-- Активные напоминания
SELECT COUNT(*) FROM telegram_reminders WHERE is_active = true;

-- Напоминания, ожидающие отправки
SELECT * FROM telegram_reminders 
WHERE is_active = true AND next_run_at <= NOW()
ORDER BY next_run_at;

-- Последние отправленные напоминания
SELECT * FROM reminder_logs 
ORDER BY sent_at DESC 
LIMIT 10;
```

## 🔐 Безопасность

- Webhook endpoint не требует аутентификации (требуется Telegram)
- Telegram верифицирует webhook при установке
- Все запросы от Telegram подписаны сертификатом Telegram

### Рекомендации по безопасности

1. **Ограничьте доступ к webhook по IP:**
   - Telegram использует диапазон IP: `149.154.160.0/20`, `91.108.4.0/22`

2. **Проверяйте заголовок `X-Telegram-Bot-Api-Secret-Token`:**
   ```javascript
   const secretToken = req.headers['x-telegram-bot-api-secret-token'];
   if (secretToken !== YOUR_SECRET_TOKEN) {
       return res.status(401).json({ ok: false, error: 'Invalid token' });
   }
   ```

## 🐛 Отладка

### Включение логирования

Модуль использует стандартный вывод в консоль с префиксом `[Reminder]`.

### Тестирование команд

1. Отправьте боту `/help` — должна появиться справка
2. Отправьте `/remind 1m Тест` — должно создаться напоминание
3. Через 1 минуту должно прийти сообщение

### Частые проблемы

| Проблема | Решение |
|----------|---------|
| Бот не отвечает на команды | Проверьте webhook через `getWebhookInfo` |
| Напоминания не приходят | Проверьте логи воркера `reminder_dispatcher` |
| Ошибка "Database not available" | Проверьте подключение к PostgreSQL |
| Webhook возвращает ошибку | Проверьте SSL сертификат домена |

## 📈 Расширение

### Добавление связи с веб-пользователем

Для отображения напоминаний в веб-интерфейсе:

1. Добавьте поле `user_id` в `telegram_users` для связи с `users.id`
2. Реализуйте endpoint для linking (например, через уникальный код)
3. Обновите `/api/reminders` для выборки по связанному пользователю

### Пример linking через код:

```javascript
// Генерация кода для привязки
const code = Math.random().toString(36).substring(7);
await db.query('INSERT INTO telegram_link_codes (code, user_id) VALUES ($1, $2)', [code, userId]);

// Команда боту /link <code>
// Поиск кода и привязка к Telegram user
```

## 📄 Лицензия

Модуль является частью проекта VadminLink.
